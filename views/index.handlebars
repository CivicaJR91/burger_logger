

<div class="container">
<div id="top">
  <div class="row">
    <div class="col-12">
  <h1>Eat-Da-Burger!</h1>
  <br>
  <img src="/asset/burger.jpg" class="img-fluid" alt="delivious burger" style="width: 300px; height:250px;">
</div>
</div>
</div>
</div>

<div class="row" id="row2">
  <div class="col-12">
    <h2 id="YB">Enter A New Burger</h2>
    <form class="create-form">
      <div class="form-group"></div>
      <label for="burger">Burger Name:</label>
      <input type="text" id="burger" name="name" style="height: 30px;">
      <button class="btn btn-secondary btn-sm" type="submit">Add Burger</button>
  </div>
  </form>
</div>
</div>

<div class="container" id="tables">
  <div class="row justify-content-center">
    <div class="col-6">
      <h2>Your Burgers</h2>
      {{#each burgers}}
      {{#unless devoured}}
      {{burger_name}}
       {{!-- {{> burgers/burger-block}} --}}
     
      {{/unless}}
      {{/each}}
      </ul>
    </div>

    <div class="col-6">
      <h2>Devoured Burgers</h2>
      <ul>
        {{#each burgers}}
        {{#if devoured}}
        <li>
          {{id}}
          {{burger_name}}
        </li>
        {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>
</div>
<ul>